# Disable Edge for Next.js middleware so it runs in Node (avoids Node crypto in Edge error)
[build.environment]
  NEXT_DISABLE_NETLIFY_EDGE = "true"
  # Évite le timeout P1002 (advisory lock) avec Neon lors de prisma migrate deploy
  PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK = "true"
